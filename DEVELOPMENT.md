# Development

## Releasing

Run locally:

```sh
NPM_TOKEN=<your-npm-token> GH_TOKEN=<your-gh-token> yarn semantic-release --no-ci
```

- GitHub token: https://github.com/settings/tokens (classic)
- npm token: https://www.npmjs.com/settings/<your-username>/tokens
